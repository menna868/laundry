import { FeaturePlaceholder } from "@/app/components/layout/FeaturePlaceholder";

export default function LaundryDriversPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Delivery Team"
      title="Driver operations are ready for the next build step."
      description="From here you can add courier assignment, live status, route optimization, and branch delivery performance without changing the navigation structure again."
      accent="orange"
    />
  );
}
